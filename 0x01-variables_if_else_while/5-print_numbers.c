#include <stdio.h>
/**
 * main - entry point
 * Description: print single digits from 0
 * Return:return 0 (Success)
*/
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
{
		printf("%d", i);
}
	printf("\n");
	return (0);
}
