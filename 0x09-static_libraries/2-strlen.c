#include "main.h"
#include <string.h>
/**
 * _strlen - returns length of a string
 * @s: input,string
 * Return: length of string
 */

int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}
