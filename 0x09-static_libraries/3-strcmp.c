#include "main.h"
#include <string.h>
/**
 * _strcmp - compare two strings
 * @s1: string input 1
 * @s2: string input 2
 * Return: 1 if true, 0 if false
 */

int _strcmp(char *s1, char *s2)
{
	int result;

	result = strcmp(s1, s2);
	return (result);
}
